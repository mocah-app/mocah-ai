// enums.prisma
// Shared enums across the application

enum PlanName {
  starter
  pro
  scale
}

enum GenerationType {
  text
  image
  template
  email
}

enum GenerationStatus {
  pending
  processing
  completed
  failed
}

enum AIModel {
  gpt_4
  gpt_4_turbo
  gpt_3_5_turbo
  dall_e_2
  dall_e_3
  claude_3_opus
  claude_3_sonnet
  claude_3_haiku
}

enum ExportFormat {
  html
  pdf
  json
  mailchimp
  sendgrid
  campaign_monitor
  constant_contact
}

enum QuotaPeriod {
  daily
  weekly
  monthly
}

enum IntegrationType {
  mailchimp
  sendgrid
  klaviyo
  hubspot
  constant_contact
  campaign_monitor
  custom_api
}

enum ImageVariationType {
  original      // First generation
  regeneration  // Same prompt, new generation
  variation     // Variation of existing image (DALL-E variations)
  upscale       // Upscaled version
  edit          // Edited/modified version
}

enum StyleType {
  INLINE              // style={{ ... }} directly on elements
  PREDEFINED_CLASSES  // className="hero-section"
  STYLE_OBJECTS       // const heroStyle = { ... }
}

enum ChatMessageRole {
  user
  assistant
}

enum TemplateStatus {
  DRAFT     // Initial state - AI generation not complete
  ACTIVE    // Generation complete, template ready to use
  ARCHIVED  // Soft archived (alternative to deletion)
}

